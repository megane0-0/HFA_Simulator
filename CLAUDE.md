作成日: 20260920
最終更新日: 20260920

# CLAUDE.md — 開発メモ

このリポジトリで作業する Claude 向けの覚書です。経緯と設計の意図を残し、次のセッションが同じ判断を再現できるようにすることが目的です。

## アプリの目的と構成

ハンフリー自動視野計 30-2（76点）の結果から、患者の見え方をカメラ映像で再現する教育用 Web アプリです。患者さん・家族・支援者への説明に使います。

- **単一ファイル構成**: `index.html` 1ファイルで完結。外部ライブラリ・ビルド手順・パッケージマネージャは使っていません。この方針は維持してください
- **公開**: `main` ブランチが GitHub Pages で https://megane0-0.github.io/HFA_Simulator/ として公開されています。main への変更は即座に公開ページへ反映されます
- **データの扱い**: 視野データはブラウザの localStorage にのみ保存され、外部送信はありません
- **【重要】医療機器ではありません**。理解促進・教育目的のツールであり、診断・治療の根拠には使えません。この注意書きをフッターから削除しないでください

## 運用ルール

- Claude は `claude/` で始まるブランチに push し、PR を作ります
- **`main` には直接 push しません**。マージはリポジトリ所有者が行います（公開ページが即座に変わるため）
- PR の本文には、変更点と確認結果を簡潔に書きます

## 実装の要点

### dB からレベルへの変換（`levelForDb`）

感度 dB を5段階の欠損レベルに落とします。境界は次のとおりです。

| レベル | dB | 意味 |
|---|---|---|
| 0 | 25以上 | 正常 |
| 1 | 15〜24 | 軽度 |
| 2 | 5〜14 | 中等度 |
| 3 | 1〜4 | 強度（形はほぼ判別不能） |
| 4 | 0以下 | 視認不能 |

入力は 0〜40dB（`DB_MAX = 40`）。レベル選択で塗ったときの代表値は `LEVEL_DB = [30, 20, 10, 3, 0]` です。

### IDW 補間（`computeGrid`）

76点の離散データを 64×64（`GRID_SIZE`）のグリッドに補間します。

- 逆距離加重（power = 2）、**近傍4点限定**（`K_NEAREST = 4`）。全点を使うと遠方の正常点に引っ張られて暗点内部が明るくなり、測定点の真上だけ暗い「まだら」になるため
- 30-2 は中心 (0,0) に測定点がないので、**最内4点 (±3, ±3) の平均値を持つ仮想中心点**を追加してから補間します。これで中心暗点が面として暗くなります
- グリッドは視野 ±30度（`FIELD_DEG = 30`）に対応します

### レベル別のピクセル化とブラー（`FILTER_PARAMS` / `drawFilteredLayer`）

| レベル | pixel（縮小率） | blur（px） |
|---|---|---|
| 1 | 8 | 4 |
| 2 | 14 | 8 |
| 3 | 32 | 24 |
| 4 | 64 | 48 |

**減光・黒塗りは使いません。** 暗点は「黒く見える」のではなく周囲の色に溶けて何も知覚できない状態なので、粗いピクセル化と強いブラーで背景色ににじませる表現にしています。レベル1→4の順に重ね、レベル4はマスクを自己合成してアルファを立ち上げ、下のシャープな映像が透けないようにしています。

マスクの縁は円形グラデーションでソフトクリップしています（`R * 0.9` から `R` にかけてフェード）。

### 左右眼のデータと両眼表示（`eyeData` / `simDb`）

- `eyeData = { od: [76], os: [76] }` を localStorage キー `hfa-sim-data-v2` に保存します。旧形式（`hfa-sim-data-v1`、両眼まとめた単一配列）からの移行処理も残っています
- 入力タブは `currentEye` で編集対象を切り替えます
- カメラタブの表示眼は `simEye`（`"od"` / `"os"` / `"both"`、キー `hfa-sim-eye-v1`）
- **両眼（better eye）** は各点で `Math.max(od[i], os[i])`、つまり感度の良い方を採用する単純な両眼視モデルです

### 画角設定（`fovShort` / `updateGeometry`）

「カメラ映像の短辺が視野何度に相当するか」を選ばせ、シミュレーション円（視野半径30度）の半径を `R = FIELD_DEG * (短辺px / fovShort)` で決めます。既定は30度、選択肢として iPhone 約43/46度、Pixel 約44度、Galaxy 約47度、Xperia 約46度を用意しています（キー `hfa-sim-fov-v1`）。円の外側は視野30度超として黒く塗ります。

### タブ切替の副作用

入力タブへ移動すると `switchTab` がカメラを停止します（「タブ移動のため停止しました」）。意図した挙動です。自動テストでプリセットを変えてからカメラ表示を撮る場合、**条件ごとにカメラを開始し直す**必要があります。

## 緑内障プリセットの設計

上方から進行する典型的な開放隅角緑内障を、初期・中期・後期の3種で表現しています（`GLAUCOMA_OD` / `glaucomaPreset`）。

### 表の持ち方

- 表は**右眼を患者から見た向き**で書いています（左が鼻側、右が耳側）
- 行は y = +27 … −27、列は x = −27 … +27。`null` は 30-2 で測定しない位置
- **左眼は x を反転して参照**します（`eye === "os" ? -x : x`）。鼻側階段・弓状暗点の向きを保つためです
- プリセット関数は `(x, y, eye)` を取ります。適用ボタンから `currentEye` を渡しています

### 正常部の値

中心32dB から周辺28dB（`N = round(33 − 0.18r)`）を正常とみなしています。周辺をレベル0の境界である25dB にすると、補間で欠損が広くにじみ出たため28dB にしました。

全76点で **正常 ≥ 初期 ≥ 中期 ≥ 後期** となるようにしてあり、進行しても感度が戻る点はありません。

### HPA分類の近似チェック

病期は Hodapp-Parrish-Anderson 分類を目安にしています。**HFA の年齢別正常データベースは使わない簡易計算**である点に注意してください。

- TD = 値 − 上記の正常値
- MD = 盲点2点を除く74点の単純平均
- GH = TD の85パーセンタイル
- 確率の目安: 偏心度10°未満で p5 ≤ −3 / p1 ≤ −5、10〜20°で −4 / −6、20°以上で −5 / −8

| 病期 | MD | p5 | p1 | 中心5° |
|---|---|---|---|---|
| 初期 | −2.03 | 10点 | 8点 | 正常 |
| 中期 | −6.95 | 28点 | 19点（基準の「20点未満」の境目） | 最小18dB |
| 後期 | −18.12 | 55点 | 51点 | — |

### 仕様として変えていない点

- **「この眼をもう一方へコピー」は反転しません。** 同名半盲などでは同じ値のコピーが正しいためです。緑内障プリセットを両眼に入れるときは、コピーではなく眼ごとに適用してください
- **両眼（better eye）表示では、片眼だけの欠損や、両眼とも初期の欠損はほぼ表示されません。** 片眼の見え方を示すときは「右眼」「左眼」を選びます
- 画角46°で横持ちにすると上下は約±23°までしか映らないため、上方の弓状暗点の外側は画面外になります

## 変更したときの確認手順

ヘッドレス Chromium（Playwright）で次を確認します。`index.html` をローカルの HTTP サーバーで配信してから開いてください（localStorage を使うため `file://` は避ける）。

### 1. 値の確認

期待値は `index.html` 内の `GLAUCOMA_OD` から独立に再計算し、アプリの出力（localStorage の `hfa-sim-data-v2`）と突き合わせます。

- プリセット7種 × 右眼・左眼で、76点すべてが期待値どおりか
- 左眼で `OS[x, y] === OD[-x, y]` が成立し、かつ右眼と同一配列になっていないか
- 既存4種（正常視野・求心性視野狭窄・中心暗点・上半視野欠損）の値が変わっていないか
- 「この眼をもう一方へコピー」が反転せず複製するか
- リロード後に76点データ・画角・表示眼が復元されるか
- `pageerror` / `console.error` が0件か
- 補間マップ（64×64）が右眼と左眼で鏡像になっているか

### 2. 疑似カメラでの撮影

標識や文字のある街の絵を用意し、Chromium の疑似カメラに入力します。

```
--use-fake-device-for-media-stream
--use-fake-ui-for-media-stream
--use-file-for-fake-video-capture=<path>/scene.y4m
```

y4m は自前で書き出せます（canvas から RGBA を取り出し、BT.601 で YUV420 に変換してヘッダ `YUV4MPEG2 W960 H540 F15:1 Ip A1:1 C420mpeg2` を付ける）。Playwright 同梱の ffmpeg は PNG デコーダを持たない簡易ビルドなので、PNG からの変換はできません。

画角は46°に設定し、次を確認します。

- **右眼のみ**: 初期・中期で画面左上（右眼の上鼻側）がぼけ、中心は明瞭。後期は上半視野がほぼ消失し、中心の小島と右下（下耳側）が残る
- **左眼のみ**: 左右が反転する
- **両眼**: 片眼だけ緑内障でもう片方が正常なら、欠損はほぼ出ない

撮影時の注意点が2つあります。

- Playwright の要素スクリーンショットは canvas の更新を拾えないことがあります。`outCanvas.toDataURL()` で直接ビットマップを取り出してください
- 前述のとおり、入力タブへ移動するとカメラが止まります。条件ごとに「カメラ開始」を押し直してください
